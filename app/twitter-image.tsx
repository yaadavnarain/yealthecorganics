// The twitter card uses the same generated image as the open graph card, so the
// two can never drift apart. Re-exported rather than duplicated.
export { default, alt, size, contentType } from "./opengraph-image";
